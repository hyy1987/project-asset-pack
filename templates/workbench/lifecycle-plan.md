# 项目全周期规划

项目 ID：{{project_id}}

生成时间：{{generated_at}}

## 规划定位

这份文件是项目全周期的上位规划，用来约束阶段计划和阶段执行。
阶段开发可以一期一期推进、一期一期评审，但不能做到哪算哪。每个阶段计划都必须回到这份全周期规划中确认位置、目标和边界。

## 项目目标

- 待 Agent 基于信息对齐稿和人工确认结果生成。

## 交付范围

### 本项目交付

- 待 Agent 生成。

### 明确不交付

- 待 Agent 生成。

## 全周期阶段路线图

| 阶段 | 阶段目标 | 主要产物 | 人工评审点 | 进入下一阶段条件 |
| --- | --- | --- | --- | --- |
| stage-1 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 |
| stage-2 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 |
| stage-3 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 | 待 Agent 生成 |

## 关键里程碑

- 待 Agent 生成。

## 技术与交付策略

- 待 Agent 生成。

## Git 仓库准备

- 仓库拆分是否已确认：待 Agent 基于规划生成。
- 需要创建或补齐的子仓库：待 Agent 基于规划生成。
- 默认远程仓库命名：`<project_id>-<repo_name>`。
- 人类确认仓库拆分和命名后，Agent 可运行：

```powershell
python scripts/init_project_repositories.py --project {{project_id}} --confirmed
```

如果暂不创建某个子仓库，必须在本节说明原因和后续触发条件。禁止覆盖已有远程仓库；如果远程仓库已存在，必须停下并交给人类判断。

## 风险、假设和依赖

- 待 Agent 生成。

## 后续规划调整规则

1. 每个阶段评审后，都要检查全周期规划是否需要调整。
2. 调整只能基于阶段结果、人工评审意见、范围变化或新发现风险。
3. 调整后必须保留“为什么调整、影响哪些阶段、是否影响交付范围”。
4. 后续阶段计划必须引用最新全周期规划。
